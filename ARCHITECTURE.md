# 📐 Curry House Jar - App Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         MOBILE APP                              │
│                   (React Native + Expo)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              PRESENTATION LAYER (Screens)               │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • Login Screen          • Home Screen                  │  │
│  │  • Register Screen       • Menu Screen                  │  │
│  │  • Cart Screen          • Orders Screen                 │  │
│  │  • Profile Screen                                       │  │
│  └─────────────────────────────────────────────────────────┘  │
│                            ↕                                   │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │         STATE MANAGEMENT LAYER (Zustand Stores)         │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • authStore      - User authentication                 │  │
│  │  • cartStore      - Shopping cart                       │  │
│  │  • menuStore      - Menu items                          │  │
│  │  • orderStore     - Order management                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                            ↕                                   │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │           SERVICE LAYER (API Integration)               │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • api.js         - Axios HTTP client                   │  │
│  │  • authAPI        - Auth endpoints                      │  │
│  │  • menuAPI        - Menu endpoints                      │  │
│  │  • ordersAPI      - Order endpoints                     │  │
│  │  • userAPI        - User endpoints                      │  │
│  └─────────────────────────────────────────────────────────┘  │
│                            ↕                                   │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │          LOCAL STORAGE (AsyncStorage)                   │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • User Token     • User Data     • Cart Data           │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↕
                    HTTP/HTTPS REST API
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND SERVER                             │
│                 (Node.js + Express.js)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              MIDDLEWARE LAYER                           │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • CORS           - Cross-origin requests               │  │
│  │  • Body Parser    - JSON parsing                        │  │
│  │  • Auth Middleware - JWT verification                   │  │
│  │  • Error Handler  - Error management                    │  │
│  └─────────────────────────────────────────────────────────┘  │
│                            ↕                                   │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │               ROUTES LAYER (API Endpoints)              │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • /api/auth      - Authentication routes               │  │
│  │  • /api/menu      - Menu management routes              │  │
│  │  • /api/orders    - Order processing routes             │  │
│  │  • /api/user      - User management routes              │  │
│  └─────────────────────────────────────────────────────────┘  │
│                            ↕                                   │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │            MODELS LAYER (Mongoose Schemas)              │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • User Model     - User schema & methods               │  │
│  │  • MenuItem Model - Menu item schema                    │  │
│  │  • Order Model    - Order schema                        │  │
│  └─────────────────────────────────────────────────────────┘  │
│                            ↕                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↕
                    MongoDB Connection
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                         DATABASE                                │
│                   (MongoDB - NoSQL)                             │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              COLLECTIONS                                │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │  • users          - User accounts & profiles            │  │
│  │  • menuitems      - Restaurant menu items               │  │
│  │  • orders         - Customer orders                     │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. User Authentication Flow

```
User opens app
      ↓
Check AsyncStorage for token
      ↓
   ┌──────────────────┐
   │  Token exists?   │
   └────┬──────┬──────┘
        │      │
       Yes    No
        │      │
        ↓      ↓
   Load user  Show Login
     data      Screen
        ↓          ↓
   Navigate   User enters
   to Home    credentials
              ↓
         POST /api/auth/login
              ↓
         Verify credentials
              ↓
         Generate JWT token
              ↓
         Return token + user data
              ↓
         Save to AsyncStorage
              ↓
         Update authStore
              ↓
         Navigate to Home
```

### 2. Order Placement Flow

```
User browses menu
      ↓
Click "Add to Cart"
      ↓
Update cartStore (Zustand)
      ↓
Save to AsyncStorage
      ↓
Navigate to Cart
      ↓
Review items
      ↓
Click "Place Order"
      ↓
Validate cart items
      ↓
Prepare order data
      ↓
POST /api/orders (with JWT token)
      ↓
Backend validates token
      ↓
Create order in MongoDB
      ↓
Generate order number
      ↓
Return order confirmation
      ↓
Clear cart
      ↓
Update orderStore
      ↓
Show success message
      ↓
Navigate to Orders screen
```

### 3. Menu Browsing Flow

```
User opens Menu screen
      ↓
fetchMenuItems()
      ↓
GET /api/menu
      ↓
MongoDB query for menu items
      ↓
Return menu items array
      ↓
Update menuStore
      ↓
Render menu items
      ↓
User applies filter/search
      ↓
Update local state
      ↓
Re-fetch with parameters
      ↓
GET /api/menu?category=X&search=Y
      ↓
Filtered results returned
      ↓
Update UI
```

---

## Security Architecture

```
┌─────────────────────────────────────────────┐
│         MOBILE APP (Client)                 │
└──────────────┬──────────────────────────────┘
               │
               │ 1. Login Request
               │    (email + password)
               ↓
┌─────────────────────────────────────────────┐
│         BACKEND API                         │
│  ┌────────────────────────────────────┐    │
│  │ 2. Validate credentials            │    │
│  │    - Check email exists            │    │
│  │    - Compare hashed password       │    │
│  └────────────────────────────────────┘    │
│               ↓                             │
│  ┌────────────────────────────────────┐    │
│  │ 3. Generate JWT Token              │    │
│  │    - userId in payload             │    │
│  │    - 30 day expiration             │    │
│  │    - Signed with secret            │    │
│  └────────────────────────────────────┘    │
└──────────────┬──────────────────────────────┘
               │
               │ 4. Return token + user data
               ↓
┌─────────────────────────────────────────────┐
│         MOBILE APP                          │
│  ┌────────────────────────────────────┐    │
│  │ 5. Store token                     │    │
│  │    - AsyncStorage (persistent)     │    │
│  │    - authStore (memory)            │    │
│  └────────────────────────────────────┘    │
│               ↓                             │
│  ┌────────────────────────────────────┐    │
│  │ 6. Subsequent API calls            │    │
│  │    Authorization: Bearer <token>   │    │
│  └────────────────────────────────────┘    │
└──────────────┬──────────────────────────────┘
               │
               │ 7. Protected API request
               ↓
┌─────────────────────────────────────────────┐
│         BACKEND API                         │
│  ┌────────────────────────────────────┐    │
│  │ 8. Verify JWT Token                │    │
│  │    - Check signature               │    │
│  │    - Check expiration              │    │
│  │    - Extract userId                │    │
│  └────────────────────────────────────┘    │
│               ↓                             │
│  ┌────────────────────────────────────┐    │
│  │ 9. Process request                 │    │
│  │    - req.user available            │    │
│  │    - Access protected resources    │    │
│  └────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

---

## State Management (Zustand)

```
┌────────────────────────────────────────────────┐
│           Zustand Store Structure              │
├────────────────────────────────────────────────┤
│                                                │
│  authStore                                     │
│  ├── State                                     │
│  │   ├── user: User object                    │
│  │   ├── token: JWT string                    │
│  │   ├── isAuthenticated: boolean             │
│  │   └── isLoading: boolean                   │
│  └── Actions                                   │
│      ├── login()                               │
│      ├── register()                            │
│      ├── logout()                              │
│      └── updateUser()                          │
│                                                │
│  cartStore                                     │
│  ├── State                                     │
│  │   ├── items: Array<CartItem>               │
│  │   ├── totalItems: number                   │
│  │   └── totalPrice: number                   │
│  └── Actions                                   │
│      ├── addItem()                             │
│      ├── removeItem()                          │
│      ├── updateQuantity()                      │
│      └── clearCart()                           │
│                                                │
│  menuStore                                     │
│  ├── State                                     │
│  │   ├── menuItems: Array<MenuItem>           │
│  │   ├── categories: Array<string>            │
│  │   ├── selectedCategory: string             │
│  │   └── searchQuery: string                  │
│  └── Actions                                   │
│      ├── fetchMenuItems()                      │
│      ├── fetchCategories()                     │
│      └── setSelectedCategory()                 │
│                                                │
│  orderStore                                    │
│  ├── State                                     │
│  │   ├── orders: Array<Order>                 │
│  │   ├── currentOrder: Order                  │
│  │   └── isLoading: boolean                   │
│  └── Actions                                   │
│      ├── createOrder()                         │
│      ├── fetchOrders()                         │
│      └── cancelOrder()                         │
│                                                │
└────────────────────────────────────────────────┘
```

---

## Database Schema

```
┌────────────────────────────────────────────────┐
│              MongoDB Collections               │
├────────────────────────────────────────────────┤
│                                                │
│  users                                         │
│  ├── _id: ObjectId                             │
│  ├── name: String (required)                   │
│  ├── email: String (unique, required)          │
│  ├── phone: String (required)                  │
│  ├── password: String (hashed, required)       │
│  ├── address: Object                           │
│  │   ├── street: String                        │
│  │   ├── city: String                          │
│  │   ├── postalCode: String                    │
│  │   └── country: String                       │
│  ├── avatar: String (URL)                      │
│  ├── role: String (user/admin)                 │
│  ├── favorites: Array<ObjectId>                │
│  ├── createdAt: Date                           │
│  └── updatedAt: Date                           │
│                                                │
│  menuitems                                     │
│  ├── _id: ObjectId                             │
│  ├── name: String (required)                   │
│  ├── description: String (required)            │
│  ├── price: Number (required)                  │
│  ├── category: String (enum, required)         │
│  ├── image: String (URL)                       │
│  ├── tags: Array<String>                       │
│  ├── isAvailable: Boolean                      │
│  ├── isPopular: Boolean                        │
│  ├── isVegetarian: Boolean                     │
│  ├── spiceLevel: String (enum)                 │
│  ├── preparationTime: Number (minutes)         │
│  ├── rating: Number (0-5)                      │
│  ├── reviewCount: Number                       │
│  ├── createdAt: Date                           │
│  └── updatedAt: Date                           │
│                                                │
│  orders                                        │
│  ├── _id: ObjectId                             │
│  ├── user: ObjectId (ref: User)                │
│  ├── orderNumber: String (unique)              │
│  ├── items: Array<OrderItem>                   │
│  │   ├── menuItem: ObjectId                    │
│  │   ├── name: String                          │
│  │   ├── price: Number                         │
│  │   ├── quantity: Number                      │
│  │   └── subtotal: Number                      │
│  ├── totalAmount: Number                       │
│  ├── deliveryAddress: Object                   │
│  ├── deliveryType: String (enum)               │
│  ├── status: String (enum)                     │
│  ├── paymentMethod: String (enum)              │
│  ├── paymentStatus: String (enum)              │
│  ├── specialInstructions: String               │
│  ├── estimatedDeliveryTime: Number             │
│  ├── createdAt: Date                           │
│  └── updatedAt: Date                           │
│                                                │
└────────────────────────────────────────────────┘
```

---

## API Endpoints Reference

```
┌────────────────────────────────────────────────┐
│          REST API Endpoints                    │
├────────────────────────────────────────────────┤
│                                                │
│  AUTHENTICATION (/api/auth)                    │
│  ├── POST   /register    - Create account     │
│  ├── POST   /login       - User login         │
│  └── GET    /me          - Current user [P]   │
│                                                │
│  MENU (/api/menu)                              │
│  ├── GET    /            - All menu items     │
│  ├── GET    /categories  - All categories     │
│  ├── GET    /:id         - Single item        │
│  └── GET    /popular/items - Popular items    │
│                                                │
│  ORDERS (/api/orders)                          │
│  ├── POST   /            - Create order [P]   │
│  ├── GET    /            - User orders [P]    │
│  ├── GET    /:id         - Single order [P]   │
│  └── PATCH  /:id/cancel  - Cancel order [P]   │
│                                                │
│  USER (/api/user)                              │
│  ├── PUT    /profile     - Update profile [P] │
│  ├── GET    /favorites   - Get favorites [P]  │
│  ├── POST   /favorites/:id - Add favorite [P] │
│  └── DELETE /favorites/:id - Remove fav [P]   │
│                                                │
│  [P] = Protected (requires JWT token)          │
│                                                │
└────────────────────────────────────────────────┘
```

---

**Legend:**

- → : Data flow direction
- ↕ : Bidirectional communication
- [P] : Protected endpoint (JWT required)
- Zustand : State management library
- JWT : JSON Web Token
- API : Application Programming Interface
